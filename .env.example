# Video2MD Environment Configuration
# Copy this file to .env and modify the values as needed.

# ============================================================================
# Whisper Local Client Configuration (New - Recommended)
# ============================================================================

# Model size to use (affects accuracy vs. speed trade-off)
# Options: tiny, base, small, medium, large-v1, large-v2, large-v3
# Recommendations:
#   - tiny: Fastest, lowest accuracy (~75 MB)
#   - base: Good balance for general use (~150 MB) [DEFAULT]
#   - small: Better accuracy (~500 MB)
#   - medium: Better accuracy (~1.5 GB)
#   - large-v3: Best accuracy (~3 GB)
WHISPER_MODEL_SIZE=base

# Compute device
# Options: auto, cpu, cuda
#   - auto: Automatically detect (uses CUDA if available) [DEFAULT]
#   - cpu: Force CPU-only mode
#   - cuda: Force GPU mode (falls back to CPU if unavailable)
WHISPER_DEVICE=auto

# Compute precision type
# Leave empty for automatic selection (recommended)
# Options:
#   - float16: Best for GPU (faster, less memory)
#   - int8: Best for CPU (50% less memory)
#   - int8_float16: Mixed precision
# WHISPER_COMPUTE_TYPE=

# Directory to store downloaded models
# Models will be cached here after first download
WHISPER_MODEL_DIR=./models/whisper

# Number of CPU threads to use (for CPU mode)
# Increase this if you have more CPU cores available
WHISPER_CPU_THREADS=4

# ============================================================================
# Logging Configuration
# ============================================================================

# Log level for the application
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# 
# - DEBUG: Very verbose, shows all details (useful for debugging)
# - INFO: Normal operation logs (recommended for general use)
# - WARNING: Only warnings and errors (quieter output)
# - ERROR: Only errors and critical issues
LOG_LEVEL=INFO

# ============================================================================
# API Keys
# ============================================================================

OPENAI_API_KEY=your_openai_api_key_here
SERPER_API_KEY=your_serper_api_key_here

# ============================================================================
# GPU Users: Additional Setup Required
# ============================================================================
# To enable GPU acceleration:
# 1. Install CUDA Toolkit from: https://developer.nvidia.com/cuda-downloads
# 2. Install PyTorch with CUDA:
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# 3. Set WHISPER_DEVICE=cuda (or auto)
# 4. Set WHISPER_COMPUTE_TYPE=float16
#
# See docs/GPU_SUPPORT.md for detailed instructions